server.port=8080
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB
# TODO: [CONFIG] 修改此处为你的 Python 服务器的实际 IP 和端口
python.model.server.url=http://localhost:8001/inference

# --- 数据库配置 ---
# 切换回 H2 以便验证 (由于本地 MySQL 连接失败)
spring.datasource.url=jdbc:h2:mem:lalmdb;DB_CLOSE_DELAY=-1;MODE=MySQL
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driverClassName=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# 注释掉 MySQL 配置
# spring.datasource.url=jdbc:mysql://127.0.0.1:3306/lalmdb?useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=UTC
# spring.datasource.username=root
# spring.datasource.password=509626
# spring.datasource.driverClassName=com.mysql.cj.jdbc.Driver
# spring.flyway.enabled=true
# spring.flyway.locations=classpath:db/migration
